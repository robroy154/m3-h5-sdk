{
  "//": "Typechecks the tests, which the build config excludes. Without this a test can drift from the API it exercises and still pass, because Vitest strips types rather than checking them. Target is raised because Vitest's own .d.ts files use private identifiers; the es5 contract H5 needs is still enforced by tsconfig.json, which is what actually emits.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "target": "es2020"
  },
  "include": ["src/**/*.ts", "tests/**/*.ts"],
  "exclude": ["node_modules", "build"]
}
